# Decision Log

Binding design choices. Read at every session start. Never contradicted without a new entry.

| ID | Date | Decision | Rationale | Constrains |
|---|---|---|---|---|
| D01 | 2026-06-12 | Data stack: martj42/international_results (CC0, via GitHub raw — no Kaggle auth) + openfootball/worldcup.json (public domain) as the two backbones; football-data.org free tier as optional cross-check; Wikipedia (CC BY-SA) for player/squad enrichment. ESPN unofficial API and all ToS-violating scraping rejected. | Both backbones verified by live pull on 2026-06-12: results land within ~1 day, licenses are unambiguous, no API keys needed. Owner constraint: zero legal risk. | All pipeline fetch scripts; attribution page. |
| D02 | 2026-06-12 | "Live" means near-live: scores, goalscorers, standings with minutes-to-hours delay. True in-match stat feeds (possession, shots) are out of scope. | Free tiers everywhere exclude real-time in-match stats; owner accepted the trade-off. | Frontend copy must say "near-live", never "live stats". |
| D03 | 2026-06-12 | No game play diagrams at all (owner dropped the feature, including the historical-replay alternative). Three.js is used for stats and prediction visualizations. StatsBomb data dropped from scope. | Live 2026 positional data is not obtainable free/legally; owner prioritized predictions + updating stats over replays. | Removes StatsBomb dependency and its license obligations; simplifies modules to stats + model only. |
| D04 | 2026-06-12 | Architecture: no backend. GitHub Actions scheduled workflow fetches data, re-runs the model, commits static JSON; Three.js static site hosted free (GitHub Pages or Cloudflare Pages). Railway not used (free tier discontinued). | $0 constraint; git history of model outputs doubles as a public, immutable audit log. | Pipeline must be pure batch; site must work from static JSON only. |
| D06 | 2026-06-12 | C01 remediation (owner: "go with (a)"): adopt the Dixon-Coles low-score correction — joint MLE of (β0, β1, ρ) on the same fit windows; score-grid cells (0,0),(0,1),(1,0),(1,1) scaled by the τ factors and the grid renormalized; the simulator samples scorelines from the adjusted grid. ONE re-run of the identical backtest. Adopt iff C01's original criteria pass (pooled log-loss < both baselines AND beats raw-Elo in ≥2 of 3 tournaments); otherwise fall back to raw-Elo headline per spec demotion rule. The unadjusted Poisson stays in the backtest tables as a reference row. | Pre-registered single fix for the diagnosed mechanism (independent Poisson under-prices draws); logged BEFORE the changed analysis ran. | wcmodel.py; modules 02–03 re-run; MODEL.md; spec amendment A1. |
| D08 | 2026-06-12 | Adopt the COLOR SCHEME of the `is-this-the-end` project into the static `docs/` site: the full light/warm "paper" palette (bg #ffffff, surface #faf9f6, text #2b2a27, border #e8e6e0, accent #57544d, muted #8f8c83) plus its epistemic status colors (hunch-yellow, growing-green, held-blue), with not_supported→warm-clay (#d98a6a) added. Applied to the current layout as a starting canvas (incl. recoloring the Three.js scene to light). Scope is palette only — layout/components to be (re)designed collaboratively, still in Three.js; NOT switching to its Next.js/React/Tailwind stack. Owner chose "full light/paper" + "apply as starting canvas" 2026-06-12. | Owner requested this color scheme; keeping the static stack preserves D04 ($0 deploy, no backend) and leaves the audited pipeline/site_data.json contract unchanged. | docs/ palette only (style.css + scene colors in main.js); layout TBD; no pipeline/model change. |
| D07 | 2026-06-12 | Fallback (b) implementation (triggered automatically by D06's rule after the DC re-run failed): headline match W/D/L odds = raw-Elo baseline (win expectancy + historical draw rate). The tournament simulator samples each match OUTCOME from those headline probabilities (so champion odds are coherent with the headline match odds), then samples a scoreline from the spec Poisson grid conditional on that outcome (needed for group GD/GF tie-breakers and in-sim Elo updates); knockout 90' draws resolved by Elo-expectancy Bernoulli per spec. Poisson scoreline heatmaps shown on the site only under an "experimental" label. Ratified by owner 2026-06-12 ("yes to raw-Elo baseline"). | The baseline that won the backtest must drive every published probability; the demoted model is used only where a generative score distribution is unavoidable, conditioned so it cannot contradict the headline outcome odds. | Module 03 re-run (C03/C04 re-challenged); site copy; MODEL.md. |
| D05 | 2026-06-12 | Third-place R32 allocation: each slot's allowed-group set is taken from the openfootball bracket (mirrors FIFA's constraints); the 8 qualified thirds are assigned to slots by a deterministic greedy matching (slots in match-number order, best-ranked eligible third whose assignment keeps a perfect matching feasible). Logged BEFORE module 03 runs. | FIFA's exact combination→allocation table is not available in machine-readable open data; this respects all published slot constraints and is deterministic/reproducible. | Module 03 simulator; documented as an approximation in MODEL.md. |
